---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.2
  name: palworldbackups.palworld.twodcube.io
spec:
  group: palworld.twodcube.io
  names:
    categories:
    - palworld
    kind: PalworldBackup
    listKind: PalworldBackupList
    plural: palworldbackups
    shortNames:
    - pwbackup
    - pwbk
    singular: palworldbackup
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.gameRef
      name: Game
      type: string
    - jsonPath: .spec.destination.type
      name: Type
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.completionTime
      name: Completed
      type: date
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: PalworldBackup is a point-in-time backup of a PalworldGame world.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: PalworldBackupSpec defines the desired state of a single
              backup.
            properties:
              destination:
                description: Destination for this backup. Defaults to a VolumeSnapshot.
                properties:
                  pvcName:
                    description: PVCName is the target backup PVC (required when Type
                      is PVC).
                    type: string
                  s3:
                    description: S3 destination configuration (required when Type
                      is S3).
                    properties:
                      accessKeyIDKey:
                        default: AWS_ACCESS_KEY_ID
                        description: AccessKeyIDKey is the Secret key for the access
                          key id.
                        type: string
                      bucket:
                        description: Bucket is the target bucket.
                        type: string
                      credentialsSecret:
                        description: |-
                          CredentialsSecret references a Secret with AWS_ACCESS_KEY_ID and
                          AWS_SECRET_ACCESS_KEY keys (or the keys named below).
                        type: string
                      endpoint:
                        description: Endpoint is the S3 endpoint URL. For AWS this
                          may be omitted.
                        type: string
                      insecureTLS:
                        default: false
                        description: InsecureTLS disables TLS verification for the
                          endpoint (test only).
                        type: boolean
                      prefix:
                        description: Prefix is an optional key prefix within the bucket.
                        type: string
                      region:
                        description: Region is the S3 region.
                        type: string
                      secretAccessKeyKey:
                        default: AWS_SECRET_ACCESS_KEY
                        description: SecretAccessKeyKey is the Secret key for the
                          secret access key.
                        type: string
                    required:
                    - bucket
                    - credentialsSecret
                    type: object
                  type:
                    default: VolumeSnapshot
                    description: Type of destination.
                    enum:
                    - VolumeSnapshot
                    - S3
                    - PVC
                    type: string
                type: object
              flushSave:
                default: true
                description: |-
                  FlushSave, when true (default), issues an RCON/REST "save" to the running
                  server and waits for it to complete before snapshotting so the backup is
                  application-consistent rather than merely crash-consistent.
                type: boolean
              gameRef:
                description: GameRef is the name of the PalworldGame to back up, in
                  the same namespace.
                type: string
              retain:
                default: false
                description: |-
                  Retain, when true, prevents retention policies from garbage-collecting
                  this backup.
                type: boolean
              ttlSecondsAfterFinished:
                description: |-
                  TTLSecondsAfterFinished, when set, deletes the PalworldBackup object (and
                  its VolumeSnapshot for snapshot backups if not retained) this many
                  seconds after completion.
                format: int32
                type: integer
            required:
            - gameRef
            type: object
          status:
            description: PalworldBackupStatus captures the observed state of a backup.
            properties:
              completionTime:
                description: CompletionTime is when the backup finished.
                format: date-time
                type: string
              conditions:
                description: Conditions represent the latest observations of the backup's
                  state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              jobName:
                description: JobName is the name of the Job performing the upload,
                  when applicable.
                type: string
              location:
                description: |-
                  Location is the URI of the stored backup (e.g. s3://bucket/key or the
                  snapshot name).
                type: string
              message:
                description: Message is a human-readable status message.
                type: string
              phase:
                description: Phase is the current backup phase.
                enum:
                - Pending
                - Saving
                - Snapshotting
                - Uploading
                - Completed
                - Failed
                type: string
              serverVersion:
                description: ServerVersion is the game build id that was backed up.
                type: string
              sizeBytes:
                description: SizeBytes is the size of the backup, when known.
                format: int64
                type: integer
              startTime:
                description: StartTime is when the backup began.
                format: date-time
                type: string
              volumeSnapshotName:
                description: VolumeSnapshotName is the created VolumeSnapshot (for
                  snapshot backups).
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
